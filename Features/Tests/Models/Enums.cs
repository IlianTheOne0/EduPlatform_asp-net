namespace TestingPlatform.Features.Tests.Models;

public enum TestStatus
{
    Draft = 0,
    Published = 1,
    Archived = 2
}

public enum QuestionType
{
    SingleChoice = 0,
    MultipleChoice = 1
}